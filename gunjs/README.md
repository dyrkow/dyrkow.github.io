# GunJS P2P Чат - Демонстрация Peer-to-Peer синхронизации

Простое демо-приложение, показывающее возможности GunJS для синхронизации данных в peer-to-peer режиме без централизованного сервера.

## 🚀 Возможности

- **Peer-to-Peer синхронизация** - данные синхронизируются напрямую между браузерами через WebRTC
- **Работа в локальной сети** - автоматическое обнаружение других устройств в той же сети
- **Реальное время** - сообщения появляются мгновенно на всех подключенных устройствах
- **Без сервера** - не требуется настройка или запуск сервера
- **Локальное хранение** - данные сохраняются локально в браузере

## 📋 Как использовать

### Локально

1. Откройте `index.html` в браузере (можно просто дважды кликнуть на файл)
2. Откройте ту же страницу на другом устройстве в той же локальной сети
3. Отправьте сообщение с одного устройства - оно появится на всех других устройствах!

### На GitHub Pages

1. Создайте новый репозиторий на GitHub
2. Загрузите файл `index.html` в корень репозитория
3. Перейдите в Settings → Pages
4. Выберите ветку `main` (или `master`) и папку `/ (root)`
5. Нажмите Save
6. Ваш сайт будет доступен по адресу: `https://<ваш-username>.github.io/<имя-репозитория>/`

## 🔧 Как это работает

GunJS использует WebRTC для установления прямых соединений между браузерами:

1. **Автоматическое обнаружение** - GunJS автоматически находит другие пиры в локальной сети
2. **WebRTC соединения** - устанавливаются прямые P2P соединения между браузерами
3. **Синхронизация данных** - все изменения в базе данных автоматически синхронизируются между всеми подключенными пирами
4. **Локальное хранение** - данные также сохраняются локально в браузере для работы офлайн

## 💡 Тестирование

Для проверки работы:

1. Откройте `index.html` в двух разных вкладках браузера
2. Или откройте на двух разных устройствах в одной Wi-Fi сети
3. Отправьте сообщение с одного устройства
4. Оно должно появиться на всех других устройствах в реальном времени

## 📝 Технические детали

- **GunJS** - децентрализованная база данных
- **WebRTC** - для P2P соединений
- **LocalStorage/Radisk** - для локального хранения данных
- **Один HTML файл** - весь код встроен в одну страницу

## 🌐 Совместимость

Работает во всех современных браузерах, поддерживающих WebRTC:
- Chrome/Edge
- Firefox
- Safari
- Opera

## 📄 Лицензия

Это демонстрационный пример. GunJS распространяется под лицензией Zlib.

